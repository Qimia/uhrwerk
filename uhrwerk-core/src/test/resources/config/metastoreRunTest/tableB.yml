name: "step-b"
partitionSize: "1h"

dependencies:
  - connectionName: "local_filesystem_test"
    tableName: "step-a"
    format: "parquet"

targets:
  - connectionName: "local_filesystem_test"
    format: "parquet"