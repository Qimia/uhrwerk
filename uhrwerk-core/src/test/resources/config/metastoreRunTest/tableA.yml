name: "step-a"
partitionSize: "1h"

sources:
  - connectionName: "local_filesystem_test"
    path: "untracked/external/loc"
    format: "csv"

targets:
  - connectionName: "local_filesystem_test"
    format: "parquet"