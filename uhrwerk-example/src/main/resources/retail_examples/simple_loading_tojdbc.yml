area: "staging"
vertical: "retail"
table: "sales"
version: "2.0"
parallelism: 1
max_bulk_size: 1
partition:
  unit: "days"
  size: 3

sources:
  - connection_name: "retail_mysql"
    path: "qimia_olap.sales"
    format: "jdbc"
    version: "1.0"
    partition:
      unit: "days"
      size: 3
    select:
      query: "SELECT * FROM <path> WHERE selling_date >= '<lower_bound>' and selling_date < '<upper_bound>'"
      column: "selling_date"

targets:
  - connection_name: "retail_mysql2"
    format: "jdbc"
